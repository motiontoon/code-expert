[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "sh -c '{ timeout 15 npx prisma db push --skip-generate --accept-data-loss 2>&1 || true; } & exec node dist/server/index.js'"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
